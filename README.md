# GrowthCode API Automation (CLI)

Un mini outil **Node.js** (CLI) pour **récupérer des données depuis une API** et les **exporter** en **JSON / CSV**.
Pensé pour l’automatisation : exports, reporting, dashboards, intégrations.

## ✅ Fonctionnalités
- CLI propre (`gc-api`)
- Auth Bearer optionnelle
- Export **JSON**, **CSV**, ou **les deux**
- Dossier de sortie `outputs/`
- Nommage avec timestamp (désactivable)
- Timeout configurable
- Retries sur erreurs réseau / 5xx

## 📦 Installation
```bash
npm install
